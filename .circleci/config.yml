version: 2
jobs:
  build:
    docker:
      - image: circleci/node:10.16.3
    steps:
      - checkout
      - run: npm install
  deploy:
    machine:
      enabled: true
    steps:
      - add_ssh_keys:
          fingerprints:
            - "95:12:e1:38:4e:1a:8e:b6:67:11:1c:3b:c9:bf:24:50"
      - run:
          name: Deploy to test server
          command: |
            ssh -o "StrictHostKeyChecking no" circledeployuser@69.67.131.10 "cd /var/www/html/testDeploy/circle-node-sample; git pull; npm install";

workflows:
  version: 2
  build-and-deploy:
    jobs:
      - build
      - deploy:
          requires:
            - build
          filters:
            branches:
              only: testserver
version: 2
jobs:
  build:
    docker:
      — image: circleci/node:10.16.3    
    working_directory: ~/circle-node-sample
    steps:
      — checkout      
      - run: 
            working_directory: ~/circle-node-sample/server
            command: npm install
            